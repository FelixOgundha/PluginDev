name: CI Workflow

on: 
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Sample unit test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run hello world script
      run: echo "hello world!"

  build:
    name: Sample build test
    runs-on: ubuntu-latest

    steps:
    - name: Run build script
      run: echo "I have a build running"
      shell: bash
